let result = 0, simbol;

while (true) {

    simbol = +prompt('ввудите сумму!');

    if (simbol === 0) break;

    let operetion = prompt ('операция!');

    switch(operation) {
        case '+':
            result += simbol; 
    }

}